# Place your resume PDF here

To add your resume:
1. Save your resume as `resume.pdf`
2. Place it in the `public` folder (create it if it doesn't exist)
3. The download button will work automatically

The path `/resume.pdf` will serve from the public folder.
